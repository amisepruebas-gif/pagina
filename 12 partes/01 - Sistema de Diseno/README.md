# 01 — Sistema de diseño

Base reutilizada por todos los entregables 02-12.

Instalación:
```bash
pnpm add clsx tailwind-merge
```
Importa `globals.css` desde `app/layout.tsx`. Las demás carpetas (02-12) reusan todo lo de aquí.